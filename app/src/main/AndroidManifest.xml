<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.prof.dz">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.prof.dz.application.MyApp"
        android:theme="@style/AppTheme">
        <activity android:name="com.less.historyscreen.frameworks.view.HistoryActivity"
            android:label="@string/history_activity_title">
        </activity>
        <activity android:name=".frameworks.view.WordDescrActivity"
            android:label="@string/description">
        </activity>

        <activity android:name=".frameworks.view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>